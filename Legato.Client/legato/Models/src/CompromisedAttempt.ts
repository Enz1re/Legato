export default class CompromisedAttempt {
    attemptId: number;

    compromisedToken: string;
    
    requestIP: string;

    username: string;

    requestDateTime: Date;

    isSelected: boolean;
}